# 原型说明与验证

更新日期：2026-09-10。打开 [交互原型](prototype.html) 即可演示。本轮调整用于评审体验方案，不代表正式应用已经实现全部交互。

## 本轮调整

- 将错过计划、已逾期统一到「待恢复」，保留原因标识和分类数量，逾期事项优先展示，可完成、跳过和重新安排。
- 今天聚焦当天待办，实际在今天完成的事项单独折叠。仅标题新增提供明确的去向提示和查看入口。
- 筛选支持真实状态选择、已选条件和清除；中文搜索在输入法组合结束后更新，避免中途重绘打断输入。
- 改期保留具体时间，校验计划与截止顺序；重复事项区分本次操作和未来安排。
- 日历提供日、周、月和日程视图，明确区分计划与截止。日视图按时间分组排序，不用卡片高度暗示持续时长。
- 历史统一展示普通与重复事项，完成、跳过可分别查看。目标支持关联新增，进度按关联普通任务的完成比例计算。
- 手机底部提供今天、日历、收件箱、待恢复、更多五个入口，清单及低频页面收纳到更多。
- 表单使用原生对话框，支持关闭、Esc 和焦点返回；次要字段折叠展示。

## 演示边界

演示时钟固定为 2026-09-09 12:00，所有「今天」「明天」和统计都以此为准。修改仅存放在页面内存，刷新后恢复示例数据，不读写正式应用数据库。

重复任务只演示已有示例的本次操作和后续安排，不模拟完整规则编辑或无限生成。备份、恢复及系列管理按钮标注为流程说明；设置不请求通知权限、不执行真实数据操作。回顾展示当天统计，完整周期切换不在本轮演示范围。

## 验证记录

已通过脚本语法、格式和 Git 差异空白检查。此前浏览器检查已确认桌面首页、仅标题新增后进入收件箱，以及重复事项改为明天后仍保留 18:30。

本轮浏览器复查未完成：最初本地服务未启动导致连接失败；启动后浏览器工具拦截了连接失败页的 data URL，未能继续操作。以下路径保留为待复查项，不计为通过：

1. 390px 手机视口：五个底部入口、更多中的清单和设置均可访问，表单及日历无横向溢出。
2. 待恢复：完成、跳过或改期后，列表和数量一致更新。
3. 筛选：中文输入、多条件组合及逐项/全部清除后，结果与条件一致。
4. 历史：完成普通及重复事项后均可查找；跳过记录独立筛选。
5. 日历：日/周/月/日程切换，只有截止的事项编辑后不会自动新增计划。
6. 目标：关联新增后数量更新，完成关联任务后进度更新，归档目标禁止新增关联。
7. 键盘：打开和关闭详情/编辑表单后焦点合理返回，隐藏表单不可访问。

## 实际页面同步（2026-09-10）

实际应用已采用本轮方案的五入口手机导航、折叠新增与筛选、已选条件清除、待恢复改期快捷操作、今天完成分区、普通与重复历史、日历日期新增与截止标注，以及目标关联新增和长列表折叠。正式应用继续提供实际备份恢复、通知和系列编辑能力。

实际页面验证：1280px 桌面首页，以及 390px 手机更多菜单、设置入口、月历数量下钻和精确时间新增表单已浏览器检查；精确时间表单下页面内容宽度等于 390px，无横向溢出。以上是实际应用检查，不替代前文列出的原型待复查项。自动化回归覆盖实际完成日期、较早重复历史、筛选全部状态和保留具体时间的单次改期。

## 页面结构与视觉重做（2026-09-10）

本轮将实际页面的展示层按原型重做：232px 白色侧栏、28px 页面标题、浅灰背景、统一浅深色变量、标题栏新增入口、左侧圆形完成按钮、紧凑任务信息及更多操作菜单。任务详情由侧边抽屉改为居中对话框，计划/截止优先展示，备注和组织信息折叠；所有页面共用新增弹窗，清单页带入当前清单，日历页带入选中日期。

样式集中在 `src/app/prototype-theme.css`，新增表单拆分为独立组件。原型中的演示日期、示例任务和“原型”标签不进入真实页面；数据继续来自已有服务和本地数据库。

已检查 1280px 桌面首页及任务列表、390px 手机新增弹窗、详情弹窗和月历。手机月历页面宽度与内容宽度均为 390px。新增弹窗日期快捷操作已经按字段分组，避免窄屏下与截止字段混排。自动化检查包含任务操作菜单以及新增弹窗开关和输入 ID 唯一性。
